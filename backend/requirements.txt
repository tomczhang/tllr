# FastAPI 核心
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic>=2.5.0
pydantic-settings==2.1.0
email-validator>=2.1.0  # 用于 EmailStr 验证

# 数据库
supabase>=2.7.0  # 升级到更新版本以兼容 httpx
psycopg2-binary==2.9.9

# 数据处理
pandas==2.1.3
numpy==1.26.2

# 金融数据
yfinance==0.2.32

# 工具
python-multipart==0.0.6
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0
httpx>=0.24.0  # HTTP 客户端

# AI (Phase 2) - 可选，MVP 阶段不需要
# 如果需要使用 AI 功能，取消注释以下行
# openai>=1.6.1
# langchain>=0.1.0
# langchain-openai>=0.0.5

# 异步任务 (未来)
# celery==5.3.4
# redis==5.0.1

